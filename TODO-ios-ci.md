# iOS CI Gaps — To Implement

- [ ] **SwiftLint** — Add linting job for Swift code style and best practices (equivalent of android-lint + android-ktlint)
- [ ] **iOS unit tests** — Add Swift-only test target and CI job (shared KMP tests already run under android-test, but Swift-side logic is untested)
